# النشر والتشغيل — GitHub Pages + Supabase

---

## ⚠️ اقرأ هذا قبل النشر العلني

النظام يستخدم مصادقة مخصّصة (اسم + رمز دخول) ولا يستخدم Supabase Auth، فلا يوجد
JWT تستطيع سياسات RLS الاعتماد عليه للتمييز بين مستخدم وآخر. النتيجة على شبكة
محلية موثوقة مقبولة، لكن على الإنترنت المفتوح:

| ما هو محميّ | ما هو مكشوف |
|---|---|
| عمود `passcode` غير قابل للقراءة إطلاقاً (منع على مستوى العمود + دالة `SECURITY DEFINER`) | **أرقام هواتف أولياء الأمور** يقرأها أي شخص يعرف الرابط عبر REST مباشرة |
| | أسماء الطلاب ومستوياتهم ونسب إنجازهم |
| | إمكانية إنشاء حساب محفّظ جديد ثم الدخول به |

`VITE_SUPABASE_ANON_KEY` يُدمج في ملفات JavaScript المنشورة — هذا تصميمه الطبيعي،
لكنه يعني أن كل ما يسمح به دور `anon` متاح للعالم.

**أرقام هواتف عائلات الطلاب بيانات شخصية.** لا تنشر النظام علناً بهذه الإعدادات.
خياراك:

1. **شبكة داخلية فقط** — شغّله على جهاز أو شبكة المكتب بلا GitHub Pages.
2. **تحصين RLS أولاً** — تحويل كل عمليات القراءة والكتابة إلى دوال
   `SECURITY DEFINER` تستقبل رمز جلسة (session token) يُصدره `login_user`
   وتتحقق منه داخل قاعدة البيانات، ثم سحب كل صلاحيات الجداول من `anon`.
   هذه هي الحزمة التالية المطلوبة — لم تُنفَّذ بعد.

النشر إلى GitHub Pages **في الوضع التجريبي** (بلا مفاتيح Supabase) آمن تماماً،
فالبيانات وهمية وتبقى في متصفح الزائر.

---

## 1. النشر على GitHub Pages

الإعداد جاهز في المشروع:

| الملف | الدور |
|---|---|
| [vite.config.js](vite.config.js) | `base` من `VITE_BASE_PATH`، وإضافة تولّد `404.html` |
| [src/main.jsx](src/main.jsx) | `basename={import.meta.env.BASE_URL}` |
| [.github/workflows/deploy.yml](.github/workflows/deploy.yml) | بناء ونشر تلقائي عند الدفع إلى `main` |

### الخطوات

```bash
git init && git add . && git commit -m "نظام متابعة إنجاز الطلاب"
git remote add origin https://github.com/<user>/<repo>.git
git push -u origin main
```

ثم في GitHub:

1. **Settings › Pages › Source** → اختر **GitHub Actions**.
2. (اختياري، للوضع الحقيقي) **Settings › Secrets and variables › Actions › New repository secret**:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

   إن تركتهما فارغين يُنشر الموقع في الوضع التجريبي ببيانات وهمية.
3. ادفع أي تغيير إلى `main` → يبني السكربت وينشر تلقائياً.

الرابط: `https://<user>.github.io/<repo>/`

### لماذا `404.html`؟

GitHub Pages لا يعرف التوجيه من جهة الخادم، فتحديث الصفحة على `/dashboard` يعيد
404. الإضافة في `vite.config.js` تنسخ `index.html` إلى `404.html` بعد كل بناء،
فيُعيد Pages التطبيق نفسه ويتولّى React Router المسار. تحقّق بعد البناء:

```bash
npm run build
ls dist/404.html
```

### CORS

Supabase يسمح بكل الأصول على REST افتراضياً، فلا حاجة لإعداد CORS.
إن حصرت الأصول في لوحة Supabase، أضف `https://<user>.github.io`.

---

## 2. جاهزية عشرين مستخدماً متزامناً

عشرون مستخدماً حمل صغير على Supabase — الخانق ليس الحمل بل **التزامن**. ما نُفِّذ:

### أ) رقم الطالب: تسلسل ذرّي بدل توليده في العميل

كان العميل يقرأ أكبر رقم موجود ثم يُدرج. مع محفّظين يضيفان في اللحظة نفسها
يحصلان على الرقم نفسه، ويفشل أحدهما (أو أسوأ: يمرّ الرقمان قبل تطبيق القيد).
الآن:

```sql
create sequence public.student_number_seq as bigint start with 2001;

create trigger students_assign_number
    before insert on public.students
    for each row execute function public.assign_student_number();
```

العميل يُدرج **بلا رقم**، وقاعدة البيانات تخصّصه داخل المعاملة. لا تكرار مهما
بلغ عدد المستخدمين، ولا استعلام يقرأ الجدول كاملاً في كل إضافة.

### ب) البث اللحظي: لا يعمل أحد على بيانات قديمة

```sql
alter publication supabase_realtime add table public.students;
```

`useStudents` يشترك في تغييرات جدول `students` ويعيد الجلب بكتم 800ms، **مع
تخطّي التحديث إن كان المستخدم يكتب داخل الجدول** حتى لا يختفي ما يكتبه تحت يده.

### ج) الفهارس

موجودة على `teacher_id` و`student_number` و`name` — وهي كل ما يلمسه الاستعلام
والبحث. لا حاجة لأكثر عند هذا الحجم.

### د) الطابور المحلي يستوعب انقطاع الشبكة

انقطاع مؤقت لأحد المستخدمين لا يُفقد تعديلاته: تُحفظ في `localStorage` وتُرفع
تلقائياً عند عودة الاتصال.

### ما لم يُعالَج — تعارض الكتابة (Last-Write-Wins)

إذا عدّل مستخدمان **نفس الطالب** في نفس الثانية، يفوز الأخير بلا تحذير. عملياً
كل محفّظ يرى طلاب حلقته وحده، فالتصادم الوحيد المحتمل بين أدمن ومحفّظ على نفس
الطالب. الحلّ إن احتجته: تحقّق تفاؤلي على `updated_at` يرفض الكتابة إن تغيّر
السجل بعد قراءته.

### حدود الخطة المجانية

عشرون مستخدماً متزامناً داخل حدود الخطة المجانية بمساحة واسعة (اتصالات قاعدة
البيانات تُدار عبر PostgREST بمجمّع اتصالات، والبث اللحظي يستهلك اتصالاً واحداً
لكل متصفح). القيد الأقرب هو عدد اتصالات Realtime المتزامنة إن تجاوزت مئات
المستخدمين — لا عشرين.

### تنبيه: الوضع التجريبي أحادي المتصفح

بيانات الوضع التجريبي في `localStorage`، فكل متصفح جزيرة منفصلة. **اختبار
التزامن يستلزم Supabase حقيقياً** — لا يمكن اختباره في الوضع التجريبي.

---

## 3. التشغيل المحلي مع Supabase

```bash
supabase start                    # انسخ API URL و anon key
cp .env.example .env              # واملأ VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY
# الصق supabase/schema.sql كاملاً في Supabase Studio > SQL Editor وشغّله
npm install && npm run dev
```

`schema.sql` آمن للتشغيل المتكرر (`if not exists` / `drop policy if exists` /
`setval` محسوبة)، فيمكن إعادة تشغيله بعد كل تحديث للمخطط.
